/**
 * cmd-pallet — sync commands to directory as frontmatter markdown
 */

import type { SyncOptions, SyncResult } from "./types.ts";

export async function syncSlashCommands(
  outDir: string,
  cwd?: string,
  options?: SyncOptions
): Promise<SyncResult> {
  throw new Error("not implemented: syncSlashCommands");
}
